# Django backend base URL
# Dev:  http://localhost:8000 (proxied via Vite, so use /api directly in dev)
# Prod: https://your-backend.onrender.com
VITE_API_URL=http://localhost:8000

# WebSocket URL for AI chat
# Dev:  ws://localhost:8000 (proxied via Vite)
# Prod: wss://your-backend.onrender.com
VITE_WS_URL=ws://localhost:8000
